name: Intégration continue

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  format:
    name: Format (Pint)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none
      - run: composer install --prefer-dist --no-interaction --no-progress
      - run: vendor/bin/pint --test

  tests:
    name: Tests (${{ matrix.base }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # Les migrations doivent passer sur les deux : SQLite sert aux tests,
        # MySQL est la base de production.
        base: [sqlite, mysql]

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: nsia_vie_sinistres
        ports: ['3306:3306']
        options: >-
          --health-cmd="mysqladmin ping -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - uses: actions/checkout@v4

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: pdo_mysql, pdo_sqlite, mbstring, intl
          coverage: none

      - run: composer install --prefer-dist --no-interaction --no-progress

      - run: cp .env.example .env && php artisan key:generate

      - name: Tests sur SQLite en mémoire
        if: matrix.base == 'sqlite'
        run: php artisan test

      - name: Migrations et tests sur MySQL 8
        if: matrix.base == 'mysql'
        env:
          DB_CONNECTION: mysql
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_DATABASE: nsia_vie_sinistres
          DB_USERNAME: root
          DB_PASSWORD: root
        run: |
          php artisan migrate --force
          php artisan db:seed --force
          php artisan migrate:fresh --force
          php artisan test

  symfony:
    name: Transposition Symfony
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: SinistresViePhp
    steps:
      - uses: actions/checkout@v4

      - uses: shivammathur/setup-php@v2
        with:
          # 8.2 est la version de l'hebergement LWS : c'est elle qui fait foi, pas
          # celle du poste. Tester plus haut laisserait passer une syntaxe refusee
          # en production.
          php-version: '8.2'
          extensions: mbstring, intl, pdo
          coverage: none

      # pdo_sqlsrv n'est pas installable sur le runner : la suite n'en a pas besoin,
      # elle est ecrite sans base. La verification contre SQL Server se fait sur le
      # poste de developpement, dont le conteneur embarque l'extension.
      - run: composer install --prefer-dist --no-interaction --no-progress --ignore-platform-req=ext-pdo_sqlsrv

      - name: Format (preréglage Symfony)
        run: vendor/bin/pint --test

      - name: Tests
        run: vendor/bin/phpunit
